import React from "react"

export default function Header() {
    return (
        <header className="header">
            <h2 className="header--title">Funkschein App</h2>
            <h4 className="header--project">UBI, SRC und LRC</h4>
        </header>
    )
}